{
  "name": "mardoc",
  "displayName": "MarDoc",
  "description": "Rich document editing and GitHub PR review in VS Code",
  "version": "0.1.4",
  "publisher": "mardoc",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "mardoc.open",
        "title": "MarDoc: Open"
      },
      {
        "command": "mardoc.openFile",
        "title": "Edit with MarDoc"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "mardoc.openFile",
          "when": "resourceExtname == .md || resourceExtname == .mdx || resourceExtname == .markdown || resourceExtname == .html || resourceExtname == .htm",
          "group": "navigation"
        }
      ],
      "editor/title/context": [
        {
          "command": "mardoc.openFile",
          "when": "resourceExtname == .md || resourceExtname == .mdx || resourceExtname == .markdown || resourceExtname == .html || resourceExtname == .htm"
        }
      ]
    },
    "keybindings": [
      { "command": "editor.action.clipboardCopyAction", "key": "ctrl+c", "mac": "cmd+c", "when": "activeWebviewPanelId == 'mardoc'" },
      { "command": "editor.action.clipboardPasteAction", "key": "ctrl+v", "mac": "cmd+v", "when": "activeWebviewPanelId == 'mardoc'" },
      { "command": "editor.action.clipboardCutAction", "key": "ctrl+x", "mac": "cmd+x", "when": "activeWebviewPanelId == 'mardoc'" },
      { "command": "undo", "key": "ctrl+z", "mac": "cmd+z", "when": "activeWebviewPanelId == 'mardoc'" },
      { "command": "redo", "key": "ctrl+shift+z", "mac": "cmd+shift+z", "when": "activeWebviewPanelId == 'mardoc'" },
      { "command": "editor.action.selectAll", "key": "ctrl+a", "mac": "cmd+a", "when": "activeWebviewPanelId == 'mardoc'" },
      { "command": "workbench.action.closeActiveEditor", "key": "ctrl+w", "mac": "cmd+w", "when": "activeWebviewPanelId == 'mardoc'" }
    ],
    "configuration": {
      "title": "MarDoc",
      "properties": {
        "mardoc.appUrl": {
          "type": "string",
          "default": "https://mardoc.app",
          "description": "Base URL of the MarDoc web app the extension loads in its webview. Set to `http://localhost:3000` to test against a local `npm run dev` server."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "npm run compile && vsce package",
    "publish": "npm run compile && vsce publish"
  },
  "devDependencies": {
    "@types/node": "25.5.2",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.0.0",
    "typescript": "^5.3.0"
  }
}
